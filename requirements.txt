# =============================================================================
# =============================================================================

# ── 核心 ML / RAG ──────────────────────────────────────────────────────────────
torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0
huggingface-hub>=0.33.4
transformers>=4.40.0
tokenizers>=0.19.0
sentence-transformers>=3.0.0
accelerate>=0.20.0
datasets>=2.14.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0

# ── LangChain 0.3 线（勿升 1.x，与 llama-index/langgraph 0.x 配套）──────────────
langchain==0.3.26
langchain-core==0.3.71
langchain-community==0.3.27
langchain-huggingface==0.3.1
langchain-openai==0.3.28
langchain-text-splitters==0.3.8
langchain-experimental==0.3.4
langchain-deepseek==0.1.4

# ── LlamaIndex 0.12 线 ─────────────────────────────────────────────────────────
llama-index==0.12.51
llama-index-core==0.12.52
llama-index-embeddings-huggingface==0.5.5
llama-index-embeddings-openai==0.3.1
llama-index-llms-openai==0.4.7
llama-index-llms-deepseek==0.1.2
llama-index-experimental==0.5.6

# ── 向量库 / 检索 ───────────────────────────────────────────────────────────────
chromadb>=0.4.0
faiss-cpu>=1.7.0
pymilvus==2.5.11
pymilvus.model==0.3.2

# ── 文档解析 ───────────────────────────────────────────────────────────────────
pypdf>=4.0.0
unstructured==0.18.11
unstructured-client==0.41.0
unstructured-inference==1.0.5
unstructured.pytesseract==0.3.15
unstructured[pdf]
opencv-python-headless==4.12.0.88
Markdown==3.8.2
openpyxl==3.1.5
pyarrow==20.0.0

# ── LLM 客户端 / 通用 ──────────────────────────────────────────────────────────
openai>=1.86.0,<2.0.0
python-dotenv>=1.0.0
requests>=2.28.0
tqdm>=4.64.0
pydantic>=2.0.0
lazy_loader==0.4
einops==0.8.1
ftfy==6.3.1
bilibili-api-python==17.3.0
lark==1.2.2

# ── Neo4j GraphRAG（图谱存储 + 检索集成；含 neo4j driver）───────────────────────
neo4j-graphrag>=1.0.0

# ── 编排（原 requirements 遗漏，2026-08-19 补；app/graph/workflow.py 使用）───────
# 0.x 线兼容 langchain-core 0.3；若将来升级 langchain 到 1.x 需同步升 langgraph 1.x
langgraph>=0.3,<1.0

# ── MCP 外部工具（阶段 3B；app/tools/mcp/ 管理器与 demo_server 均依赖）───────────
mcp==2.0.0

# ── Web API ────────────────────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# ── PostgreSQL / SQLAlchemy ────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1
psycopg2-binary==2.9.10

# ── Redis ──────────────────────────────────────────────────────────────────────
redis>=4.5.0,<5.0.0
arq==0.26.0

# ── MinIO ──────────────────────────────────────────────────────────────────────
minio==7.2.13

# ── JWT 认证 ───────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
python-multipart==0.0.18

# ── 工具 ───────────────────────────────────────────────────────────────────────
httpx==0.28.1
